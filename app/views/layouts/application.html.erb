<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Curatela Legal Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Curatela Legal Rails">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap 5.3.3 - Latest version via CDN %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body style="background-color: #ffffff; color: #212529; cursor: default !important;">
    <nav style="background-color: #f8f9fa; padding: 1rem; border-bottom: 1px solid #dee2e6;">
      <div style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center;">
        <div>
          <%= link_to "Curatela Legal", root_path, style: "text-decoration: none; color: #212529 !important; font-weight: bold; font-size: 1.25rem; cursor: pointer;" %>
        </div>
        <div>
          <% if user_signed_in? %>
            <%= link_to "Usuários", users_path, style: "margin-right: 1rem; text-decoration: none; color: #007bff !important; cursor: pointer;" %>
            <%= link_to "Meu Perfil", user_path(current_user), style: "margin-right: 1rem; text-decoration: none; color: #007bff !important; cursor: pointer;" %>
            <%= link_to "Sair", destroy_user_session_path, data: { turbo_method: :delete }, style: "text-decoration: none; color: #dc3545 !important; cursor: pointer;" %>
          <% else %>
            <%= link_to "Entrar", new_user_session_path, style: "margin-right: 1rem; text-decoration: none; color: #007bff !important; cursor: pointer;" %>
            <%= link_to "Registrar", new_user_registration_path, style: "text-decoration: none; color: #28a745 !important; cursor: pointer;" %>
          <% end %>
        </div>
      </div>
    </nav>

    <main style="max-width: 1200px; margin: 2rem auto; padding: 0 1rem; background-color: #ffffff; color: #212529;">
      <% if notice %>
        <div style="background-color: #d4edda; color: #155724 !important; padding: 1rem; border-radius: 0.25rem; margin-bottom: 1rem; border: 1px solid #c3e6cb;">
          <%= notice %>
        </div>
      <% end %>

      <% if alert %>
        <div style="background-color: #f8d7da; color: #721c24 !important; padding: 1rem; border-radius: 0.25rem; margin-bottom: 1rem; border: 1px solid #f5c6cb;">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
